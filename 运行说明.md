# 前后端运行说明

## 一、环境要求

- **后端**：JDK 8、Maven、MySQL（端口 3306）
- **前端**：Node.js（建议 18+）、npm

## 二、后端配置与启动

### 1. 数据库

- 确保 MySQL 已启动。
- 后端 `application.yml` 中默认配置：
  - 地址：`localhost:3306`
  - 数据库：`lamp`（不存在时会自动创建：`createDatabaseIfNotExist=true`）
  - 用户名：`root`
  - 密码：`123456`

若你本地 MySQL 用户名/密码不同，请修改：

`D:\bishe\lamp_back\src\main\resources\application.yml` 中的 `spring.datasource.username` 和 `spring.datasource.password`。

### 2. 启动后端

在**第一个终端**中执行：

```bash
cd D:\bishe\lamp_back
mvn spring-boot:run
```

或在 IDEA 中运行主类：`com.lamp.LampApplication`。

看到类似 `Tomcat started on port(s): 8080` 即表示后端已启动，接口基础地址为：**http://localhost:8080/api**。

---

## 三、前端启动

在**第二个终端**中执行：

```bash
cd D:\bishe\lamp_front
npm run dev
```

看到 `Local: http://localhost:5173/` 后，在浏览器打开：**http://localhost:5173**。

前端会把所有以 `/api` 开头的请求代理到 `http://localhost:8080`，因此登录、考勤、预约等接口会转发到后端。

---

## 四、访问方式小结

| 说明           | 地址                    |
|----------------|-------------------------|
| 前端页面       | http://localhost:5173   |
| 后端接口基础地址 | http://localhost:8080/api |

**注意**：先启动后端，再启动前端；使用系统时保持两个终端都在运行。
